# 自动售货机管理系统 - 部署指南

## 📋 目录

1. [环境要求](#环境要求)
2. [快速开始（推荐）](#快速开始推荐)
3. [详细步骤](#详细步骤)
4. [常见问题](#常见问题)
5. [停止和管理](#停止和管理)

---

## 环境要求

### 必需

- **macOS 系统**（Intel 或 Apple 芯片均可）
- **Docker Desktop**（免费软件）

### 不需要

- ❌ 不需要安装 Java
- ❌ 不需要安装 Maven
- ❌ 不需要安装 MySQL
- ❌ 不需要任何编程环境配置

**所有依赖都在 Docker 容器中运行！**

---

## 快速开始（推荐）

### 方式一：有 VPN 的情况

```bash
# 1. 进入项目目录
cd 自动售货机管理系统_副本2

# 2. 给脚本添加执行权限
chmod +x quick-start.sh

# 3. 一键启动（开着VPN）
./quick-start.sh
```

**等待 2-5 分钟**，浏览器会自动打开管理后台。

---

### 方式二：没有 VPN 的情况

```bash
# 1. 进入项目目录
cd 自动售货机管理系统_副本2

# 2. 给脚本添加执行权限
chmod +x setup-docker-mirror.sh quick-start.sh

# 3. 配置 Docker 国内镜像源
./setup-docker-mirror.sh
# 按提示重启 Docker Desktop

# 4. 等待 Docker Desktop 重启完成（约30秒）

# 5. 一键启动
./quick-start.sh
```

**等待 2-5 分钟**，浏览器会自动打开管理后台。

---

## 详细步骤

### 第一步：安装 Docker Desktop

#### 1.1 判断 Mac 芯片类型

点击屏幕左上角  → **关于本机** → 查看"芯片"

- **Apple M1/M2/M3** → Apple 芯片
- **Intel Core i5/i7/i9** → Intel 芯片

#### 1.2 下载 Docker Desktop

**官网（自动识别）**：
```
https://www.docker.com/products/docker-desktop/
```

**直接下载**：
- Apple 芯片：https://desktop.docker.com/mac/main/arm64/Docker.dmg
- Intel 芯片：https://desktop.docker.com/mac/main/amd64/Docker.dmg

#### 1.3 安装

1. 打开下载的 `.dmg` 文件
2. 将 **Docker** 图标拖到 **Applications** 文件夹
3. 打开 **Launchpad**，找到 **Docker**，点击运行
4. 等待启动，菜单栏会出现小鲸鱼图标🐳
5. 看到 "Docker Desktop is running" 表示成功

---

### 第二步：获取项目文件

将整个项目文件夹复制到你的 Mac 上：

- 方式1：AirDrop
- 方式2：U盘
- 方式3：网盘下载（百度网盘、iCloud等）

**重要**：确保复制的是整个文件夹，包含所有文件！

---

### 第三步：配置 Docker 镜像源（可选，但推荐）

如果没有 VPN，强烈建议配置国内镜像源，否则下载镜像会很慢。

```bash
# 进入项目目录
cd ~/Desktop/自动售货机管理系统_副本2

# 添加执行权限
chmod +x setup-docker-mirror.sh

# 运行配置脚本
./setup-docker-mirror.sh
```

按照提示操作：
1. 脚本会自动配置3个国内镜像源
2. 询问是否重启 Docker Desktop，输入 `y`
3. 等待 30 秒让 Docker Desktop 重启完成

---

### 第四步：一键启动项目

```bash
# 确保在项目目录中
cd ~/Desktop/自动售货机管理系统_副本2

# 添加执行权限（如果还没添加）
chmod +x quick-start.sh

# 启动项目
./quick-start.sh
```

**脚本会自动完成以下操作**：

1. ✅ 检查 Docker 环境
2. ✅ 检查端口占用
3. ✅ 启动 MySQL 数据库容器
4. ✅ 启动后端应用容器
5. ✅ 等待服务启动
6. ✅ 验证服务健康状态
7. ✅ 打开前端管理页面

**首次运行需要下载镜像（约 5 分钟）**
**后续启动只需 30 秒**

---

### 第五步：登录系统

浏览器会自动打开管理后台，使用以下账号登录：

**测试账号**：
- 用户名：`admin`
- 密码：`admin123`

---

## 常见问题

### Q1: Docker 下载镜像很慢怎么办？

**原因**：Docker Hub 在国外，国内访问慢

**解决方案**：
1. 开启 VPN
2. 或者运行 `./setup-docker-mirror.sh` 配置国内镜像源

---

### Q2: 提示端口 8080 或 3306 被占用

**原因**：本地有其他程序占用了这些端口

**解决方案**：

**查看占用进程**：
```bash
lsof -i :8080  # 查看8080端口
lsof -i :3306  # 查看3306端口
```

**停止占用程序**：
- 如果是之前启动的容器：`docker-compose -p vending down`
- 如果是本地 MySQL：关闭 MySQL 服务

---

### Q3: 启动后提示数据库连接失败

**原因**：MySQL 容器还没完全启动

**解决方案**：
1. 等待 30-60 秒
2. 重启应用容器：
   ```bash
   docker-compose -p vending restart app
   ```

---

### Q4: 浏览器提示"网络错误"或"无法连接"

**原因**：后端服务未启动或启动失败

**排查步骤**：

1. **检查容器状态**：
   ```bash
   docker ps | grep vending
   ```
   应该看到 2 个容器在运行

2. **查看应用日志**：
   ```bash
   docker logs vending-app
   ```
   查看是否有报错

3. **手动测试后端**：
   ```bash
   curl http://localhost:8080/api/auth/health
   ```
   应该返回：`{"status":"ok","message":"自动售货机管理系统运行正常"}`

---

### Q5: 首次启动很慢，卡在下载镜像

**原因**：需要从 Docker Hub 下载约 500MB 的镜像

**解决方案**：
- 耐心等待（3-10分钟，取决于网速）
- 或者配置镜像源后重新启动

---

### Q6: Docker Desktop 启动失败

**可能原因**：
- 系统版本过低（需要 macOS 10.15+）
- 虚拟化未开启（Apple 芯片无此问题）

**解决方案**：
- 更新 macOS 到最新版本
- Intel Mac 需要在 BIOS 中开启虚拟化

---

## 停止和管理

### 停止服务

```bash
# 方式1：停止并删除容器（推荐）
docker-compose -p vending down

# 方式2：只停止，不删除
docker-compose -p vending stop
```

### 重启服务

```bash
# 重启所有服务
docker-compose -p vending restart

# 只重启应用（不重启数据库）
docker-compose -p vending restart app
```

### 查看日志

```bash
# 查看应用日志
docker logs vending-app -f

# 查看数据库日志
docker logs vending-mysql -f

# 查看所有日志
docker-compose -p vending logs -f
```

### 查看容器状态

```bash
docker ps | grep vending
```

### 完全清理（删除所有数据）

```bash
# 停止并删除容器、网络、卷
docker-compose -p vending down -v

# 删除镜像（可选）
docker rmi vending-app mysql:8.0
```

---

## 访问信息

启动成功后，可以访问：

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端管理后台 | `frontend/admin.html` | 双击打开或自动打开 |
| 后端API | http://localhost:8080/api | RESTful API 接口 |
| 健康检查 | http://localhost:8080/api/auth/health | 验证服务状态 |
| MySQL数据库 | localhost:3306 | 用户名root，密码123456 |

---

## 系统架构说明

```
┌─────────────────────────────────────────────────┐
│               前端 (admin.html)                  │
│         原生 HTML + CSS + JavaScript             │
└────────────────┬────────────────────────────────┘
                 │ HTTP 请求
                 │ JWT 认证
                 ↓
┌─────────────────────────────────────────────────┐
│          后端 API (Spring Boot)                  │
│        运行在 Docker 容器 (端口8080)              │
│  ┌──────────────────────────────────────────┐   │
│  │  Spring Security + JWT                   │   │
│  │  RESTful API 接口                        │   │
│  │  业务逻辑层                               │   │
│  └──────────────┬───────────────────────────┘   │
└─────────────────┼───────────────────────────────┘
                  │ JPA/Hibernate
                  ↓
┌─────────────────────────────────────────────────┐
│         MySQL 8.0 数据库                         │
│        运行在 Docker 容器 (端口3306)              │
│  ┌──────────────────────────────────────────┐   │
│  │  用户、设备、订单、库存等数据表            │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

**优势**：
- ✅ 前后端完全分离
- ✅ 所有依赖在容器中，不污染本机环境
- ✅ 一键启动，无需配置
- ✅ 跨平台（macOS、Windows、Linux）

---

## 技术支持

如果遇到其他问题，请提供以下信息：

1. 运行环境：
   ```bash
   sw_vers  # macOS 版本
   docker --version  # Docker 版本
   ```

2. 容器状态：
   ```bash
   docker ps -a | grep vending
   ```

3. 应用日志：
   ```bash
   docker logs vending-app --tail 100
   ```

---

**最后更新**：2026-01-17
